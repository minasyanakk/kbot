# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
Image:
  registry: "ghcr.io"
  repository: "minasyanakk/kbot"
  tag: "v1.0.0-106879e"
  os: linux
  arch: amd64
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
image:
  tag: 1.0.0-8e67d51
